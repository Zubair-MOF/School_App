<div class="container">
    <h1>Hello Dashboard</h1>
    <div class="row ">
        <div class="button offset-10">
            <a href="/teacher/create"><button class="btn btn-info">+ Add Student</button></a>
        </div>
    </div>
    <table class="table table-hover">
        <thead>
          <tr>
            <th scope="col">ID</th>
            <th scope="col">First Name</th>
            <th scope="col">Last Name</th>
            <th scope="col">Age</th>
          </tr>
        </thead>
        <tbody>
          
          <% for ( teacher of teachers) { %>
            <tr>
              <td><%= teacher.id %></td>
              <td><%= teacher.name %></td>
              <td><%= teacher.last %></td>
              <td><%= teacher.age %></td>
            </tr>
          <% } %>
        </tbody>
      </table>
</div>
